# LeafMC Website — Claude Instructions

## Git Rules

- **Never push to the remote repository automatically.**
- Only run `git push` when the user explicitly asks to push.
- Committing locally is fine; pushing is not unless requested.
