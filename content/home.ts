// Content is now loaded at request time from config/home.json (+ locale overlays).
// Import types from here; import getHomeConfig from @/lib/config to read values.
export type { HomeConfig } from '@/lib/config'
