---
title: Gale World Default Config
description: The structure and default config values of Gale's world defaults config (config/gale-world-defaults.yml).
---

import galeWorldDefaultsConfig from './data/gale-world-defaults-latest'

# Gale World Default Config

The YAML config below shows the structure and default config values of Gale's world defaults config (`config/gale-world-defaults.yml`)

Click arrow button behind the config node to show description of it

<ConfigViewer config={galeWorldDefaultsConfig} />
